namespace Breakout.LevelLoading {
    /// <summary>
    /// Section Types identifying different section header types
    /// </summary>
    public enum SectionType {
        Map,
        Meta,
        Key
    }
}